fastapi==0.115.0
uvicorn[standard]==0.30.6
groq>=0.13.0,<1.0.0
httpx==0.27.0
pydantic==2.8.2
python-dotenv==1.0.1
supabase>=2.10.0,<3.0.0
openai>=1.50.0,<2.0.0

# Contextual Danish POS tagging (see services/pos_tagger.py). Plain spaCy's
# small Danish pipeline, not dacy: dacy's transformer stack (spacy-transformers
# + torch + the model weights) exceeded this instance's 512MB memory limit
# during startup and also pulls in spacy-experimental, which has no working
# build past Python 3.11. da_core_news_sm needs neither — pure spaCy, small
# footprint, confirmed working on Python 3.12.
spacy>=3.7,<3.8
https://github.com/explosion/spacy-models/releases/download/da_core_news_sm-3.7.0/da_core_news_sm-3.7.0-py3-none-any.whl
