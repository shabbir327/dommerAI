fastapi==0.115.0
uvicorn[standard]==0.30.6
groq>=0.13.0,<1.0.0
httpx==0.27.0
pydantic==2.8.2
python-dotenv==1.0.1
supabase>=2.10.0,<3.0.0

# Contextual Danish POS tagging (see services/pos_tagger.py). Using plain
# spaCy's small Danish pipeline rather than dacy: dacy pulls in
# spacy-experimental (for its biaffine parser), whose bundled C-extension
# hardcodes CPython's internal, unstable struct layout and fails to build
# from source on newer Python versions (e.g. "_PyCFrame has no member named
# use_tracing" on 3.12+). da_core_news_sm needs none of that — it's pure
# spaCy, pinned to a version with a published Python 3.12 wheel.
spacy>=3.7,<3.8
https://github.com/explosion/spacy-models/releases/download/da_core_news_sm-3.7.0/da_core_news_sm-3.7.0-py3-none-any.whl
