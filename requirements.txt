fastapi==0.115.0
uvicorn[standard]==0.30.6
groq>=0.13.0,<1.0.0
httpx==0.27.0
pydantic==2.8.2
python-dotenv==1.0.1
supabase>=2.10.0,<3.0.0

# Contextual Danish POS tagging (see services/pos_tagger.py). Now that
# .python-version is pinned to 3.11.15, dacy's spacy-experimental dependency
# has real prebuilt wheels (it has none for 3.12+, which is why this was
# switched to plain spaCy earlier — see git history for that failure).
# dacy pulls in spaCy itself as a dependency, so the fallback spacy_small
# path (POS_TAGGER_MODEL=spacy_small) keeps working from the same install.
dacy>=2.7,<3.0
https://github.com/explosion/spacy-models/releases/download/da_core_news_sm-3.7.0/da_core_news_sm-3.7.0-py3-none-any.whl
